<div{{attributes}}>
    {{ form_start(form) }}
        {{ form_row(form.teaOrCoffee) }}
        {% if form.teaType is defined %}
            {{ form_row(form.teaType) }}
        {% endif %}
        {% if form.coffeType is defined %}
            {{ form_row(form.coffeType) }}
        {% endif %}
        {{ form_row(form.sugar) }}
    {{ form_end(form) }}
</div>